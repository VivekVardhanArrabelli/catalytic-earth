# Panel acquisition recovery — 2026-09-09

Parent owns Git and cooperative lock. Owner:
`01a087fd-5a80-7dd0-be10-dbc798cab4df:ac399f3e-fb18-4b3c-87eb-5a545c0ad8bf`.
UTC start 21:05:39; base `fab4c17aeda383d3dcd32c68e99c35663239ef90`;
branch `codex/panel-acquisition-recovery-20260909`.

Question: can exact local traces for `atlas50.computational-panel-review.2026-09-05`
establish the complete cumulative acquisition ledger? Expected gain is either
an attributable remaining balance or a named authority gap. Stop if request
coverage, URL/status/response bytes or attribution remains incomplete. Partial
hits grant no acquisition. Zero new scientific-source requests are authorized
by this recovery run.

Alternative considered: further M0081 mechanism drafting would add distinct
concerted chemistry, but 3CSM metadata cannot supply that mechanism. A narrow
local retained-source feasibility check may establish whether usable direct
evidence exists; no repeat geometry wrapper or speculative chemical map.

| Lane | Bounded assignment | State |
| --- | --- | --- |
| Parent | Original task identification, accounting reconciliation and final adjudication | Findings integrated; local verification passed |
| Source | Trace provenance and attributable acquisition evidence | Complete; final packet accepted after corrections |
| Chemical representation | Retained M0081 direct-mechanism source feasibility | Complete; no concerted-event transcription supported |
| Adversarial | Accounting completeness and invalid headroom inferences | Complete; final packet accepted after corrections |

All lanes are computational reviews with correlated-error risk. Workers are
read-only; only the parent edits this board and owns publication. No independent
human or laboratory review is implied.

## Findings and parent adjudication

- The original root and two relevant worker traces are located. The fresh
  source-challenge spawn failed; the existing `forward_path` worker performed
  the challenge. Three ordered example web calls preserve ten open targets,
  not ten proven HTTP requests. Missing transport and batch-allocation fields
  prevent a complete balance. A full traffic census is not claimed.
- The panel's twelve receipt objects expand to 21 URL captures /1,125,005 bytes.
  The M0970 appendix adds seven indexed attempts /349,400 bytes, including an
  empty 404. The carried 28/1,474,405 is only a metered lower bound. Adjacent
  crosswalk accounting is not silently added. Two old reports now qualify
  unsupported total-response/below-ceiling wording; pinned inputs are unchanged.
- The original 721,603-byte panel download was written to
  `/tmp/atlas50-mcsa-selected40.json`. It is absent at that exact path; the
  producer trace contains a partial first-record printout, not the complete
  M0081 body. No exhaustive search of all possible backups is claimed.
- The retained May graph preserves M0081 conformation/electrostatic summary
  text and residue-role projections, but not the needed source proposal/steps.
  M0474's additional single-step/bond-order wording belongs to another entry.
  Parent rejects transferring it into M0081 or inferring atom correspondence
  from the shared enzyme name or 3CSM inhibitor. No new atlas object is added.
- Parent narrows the representation lane's compiler statement: exact source
  atom/event transcription needs exact evidence, but the existing compiler
  explicitly allows unavailable schemes and flow abstention for partial
  drafts. No new universal MRV admission gate, wrapper or chemical primitive
  is introduced. This investigation found a source gap, not a runtime gap.

The compact audit and input/trace pins are in
`data/governance/acquisition_audits/atlas50_panel_20260909.json`. Raw private
traces stay local; public readers cannot independently reproduce that portion
from the published overlay. Zero new scientific-source requests or bytes.

## Stop and next action

Stop recovery at the demonstrated missing transport telemetry. Do not repeat
this scan without a new exact locator. A complete attributable original ledger
or explicit owner revision of the inherited batch's acquisition authority is
needed before obtaining M0081 proposal/step evidence. Expected gain is an
accountable decision on the concerted-event draft; stop with no request if the
prerequisite is absent. Other allowed retained-source work is not globally
blocked. No consequential alternative was identified within this M0081 audit.

Reconsideration: no case-specific code; reuse exposed loss of source detail
in the old normalization; no measured curation saving or incumbent advantage.
This is a bounded recovery result and prevented unsupported transfer, not
new biological evidence or a performance claim.

## Verification and publication

Final source/diff review accepts the bounded result after correcting twelve
receipt objects versus twenty-one URL captures and separating audit start from
completion timestamps. Audit arithmetic, input/trace hashes, repository
contracts and protected-surface preservation checks pass. All workers have
finished. Only the parent
will stage, commit, push and merge; required CI must pass at the reviewed head.
The Git-local receipt records exact final head/PR/CI/synchronization/release,
without a follow-up self-hash commit.
